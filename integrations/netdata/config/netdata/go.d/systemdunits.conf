# systemdunits.conf — enable the go.d systemdunits collector.
# Metrics feed health.d/systemdunits.conf. Services + sockets + timers cover
# almost every lifecycle-relevant unit without drowning in scope units.

jobs:
  - name: units
    include:
      - "*.service"
      - "*.socket"
      - "*.timer"
