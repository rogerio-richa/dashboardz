# journald size cap. On a small VM, journald's default cap (10% of the
# filesystem) can quietly grow to a gigabyte or more. 200M keeps days of
# recent logs for debugging; anything longer-lived is the watchers'/
# dashboardz journal feed's job, not the local disk's.
[Journal]
SystemMaxUse=200M
