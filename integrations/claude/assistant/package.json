{
  "name": "dashboardz-assistant",
  "version": "0.1.0",
  "description": "Resident Claude assistant daemon for a Dashboardz hub: reminders, ask escalation, agent sessions",
  "license": "MIT",
  "type": "module",
  "bin": { "dbz-assistant": "dist/cli.js" },
  "scripts": { "test": "vitest run", "build": "tsc -p tsconfig.json", "typecheck": "node ../../../scripts/check-typecheck-scope.mjs && tsc -p tsconfig.check.json" },
  "engines": { "node": ">=22" },
  "dependencies": { "@anthropic-ai/claude-agent-sdk": "^0.3.229" },
  "devDependencies": { "@types/node": "^22.20.1", "typescript": "^7.0.2", "vitest": "^3.2.7" }
}
